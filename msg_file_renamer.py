

def main():
    # Hier kommt die Logik für das Umbenennen von MSG-Dateien
    #file_path = 'example.msg'
    #metadata = get_metadata(file_path)  # Diese Zeile bleibt gleich, wenn die Funktion in msg_handling.py existiert
    #new_name = f"renamed_{metadata['name']}.msg"  # Beispiel für neue Namensgebung
    #rename_file(file_path, new_name)
    #process_msg_file(file_path)
    pass

if __name__ == '__main__':
    main()